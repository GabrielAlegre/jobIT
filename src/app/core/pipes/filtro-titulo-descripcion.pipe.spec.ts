import { FiltroTituloDescripcionPipe } from './filtro-titulo-descripcion.pipe';

describe('FiltroTituloDescripcionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTituloDescripcionPipe();
    expect(pipe).toBeTruthy();
  });
});
