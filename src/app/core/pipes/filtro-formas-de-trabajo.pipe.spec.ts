import { FiltroCheckboxesEnums } from './filtro-checkboxes-enums.pipe';

describe('FiltroCheckboxesEnumsPipe', () => {
    it('create an instance', () => {
        const pipe = new FiltroCheckboxesEnums();
        expect(pipe).toBeTruthy();
    });
});
