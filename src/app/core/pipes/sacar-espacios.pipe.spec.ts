import { SacarEspaciosPipe } from './sacar-espacios.pipe';

describe('SacarEspaciosPipe', () => {
  it('create an instance', () => {
    const pipe = new SacarEspaciosPipe();
    expect(pipe).toBeTruthy();
  });
});
