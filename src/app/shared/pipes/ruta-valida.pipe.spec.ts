import { RutaValidaPipe } from './ruta-valida.pipe';

describe('RutaValidaPipe', () => {
  it('create an instance', () => {
    const pipe = new RutaValidaPipe();
    expect(pipe).toBeTruthy();
  });
});
