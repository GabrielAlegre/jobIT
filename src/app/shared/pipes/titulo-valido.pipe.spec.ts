import { TituloValidoPipe } from './titulo-valido.pipe';

describe('TituloValidoPipe', () => {
  it('create an instance', () => {
    const pipe = new TituloValidoPipe();
    expect(pipe).toBeTruthy();
  });
});
